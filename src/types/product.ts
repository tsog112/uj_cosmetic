// Re-export everything from the canonical types file
export type { Product, CartItem, Category, OrderItem, Order, OrderStatus, SiteSettings, AppUser } from './index';
export { formatPrice, CATEGORIES, getCategoryName, DEFAULT_SETTINGS } from './index';
